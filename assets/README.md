# Icônes de l’application

Ajoute les fichiers suivants avant une diffusion publique :

- `icon.ico` pour Windows ;
- `icon.icns` pour macOS ;
- `icon.png` (512 × 512 ou plus) pour Linux.

La configuration Forge utilise le chemin `assets/icon` et sélectionne automatiquement l’extension appropriée.
